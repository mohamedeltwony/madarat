/**
 * CSS variables exported as JavaScript
 * This replaces the :export syntax in SCSS files which is not fully supported by Turbopack
 */

const variables = {
  progressbarColor: '#1a365d', // Same as $color-primary in _variables.module.scss
};

export default variables;
