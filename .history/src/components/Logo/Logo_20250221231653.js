import styles from './Logo.module.scss';

const Logo = () => {
  return (
    <div className={styles.logo}>
      <span className={styles.text}>WeSeek Travel</span>
    </div>
  );
};

export default Logo; 